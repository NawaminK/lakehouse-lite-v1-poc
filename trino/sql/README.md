# Trino SQL Workspace

Use this folder for ad hoc or experimental Trino SQL that should not become part of the automated scenario pack yet.

Automated validation SQL belongs in:

```text
sql/
sql/scenarios/
```

Keep committed SQL deterministic and safe to run repeatedly.

